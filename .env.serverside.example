# ============================================================================
# Aevov ServerSideUp Docker PHP Environment Configuration
# ============================================================================
#
# SECURITY WARNING:
#   1. Copy this file to .env.serverside before use
#   2. NEVER commit .env.serverside to version control
#   3. Generate secure passwords: openssl rand -base64 32
#   4. These defaults are for LOCAL DEVELOPMENT ONLY
#
# Usage: cp .env.serverside.example .env.serverside
# ============================================================================

# ------------------------------------------------------------------------------
# MySQL Configuration
# ------------------------------------------------------------------------------
# CHANGE THESE! Generate with: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=<CHANGE_THIS_GENERATE_SECURE_PASSWORD>
MYSQL_DATABASE=wordpress
MYSQL_USER=aevov_user
MYSQL_PASSWORD=<CHANGE_THIS_GENERATE_SECURE_PASSWORD>
MYSQL_PORT=3307

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
REDIS_PORT=6380

# ------------------------------------------------------------------------------
# WordPress Configuration
# ------------------------------------------------------------------------------
WORDPRESS_PORT=8080
WORDPRESS_TABLE_PREFIX=wp_
WORDPRESS_DEBUG=true

# ------------------------------------------------------------------------------
# phpMyAdmin Configuration
# ------------------------------------------------------------------------------
PHPMYADMIN_PORT=8082

# ------------------------------------------------------------------------------
# PHP Configuration (via ServerSideUp)
# ------------------------------------------------------------------------------
PHP_MEMORY_LIMIT=512M
PHP_MAX_EXECUTION_TIME=300
PHP_UPLOAD_MAX_FILE_SIZE=64M
PHP_POST_MAX_SIZE=64M

# ------------------------------------------------------------------------------
# Development Settings
# ------------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=aevov-serverside
COMPOSE_FILE=docker-compose.serverside.yml
